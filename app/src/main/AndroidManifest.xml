<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.fiannce.bawei.a1809fiannce">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".FiannceApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- <activity android:name=".MainActivity"/> -->
        <activity
                android:name=".event.EventActivity"
                android:screenOrientation="portrait"/>
        <activity
                android:name=".startmode.AActivity"
                android:launchMode="singleTask"/>
        <activity android:name=".startmode.BActivity"/>
        <activity
                android:name=".startmode.CActivity"
                android:launchMode="singleTop"/>
        <activity android:name=".connect.ConnectActivity"/>
        <activity android:name=".leak.MemActivity"/>
        <activity android:name=".leak.ThreadActivity"/>
        <activity android:name=".leak.MVPActivity"/>
        <activity android:name=".adapter.AdapterActivity">
        </activity>

        <meta-data
                android:name="design_width_in_dp"
                android:value="360"/>
        <meta-data
                android:name="design_height_in_dp"
                android:value="640"/>

        <activity android:name=".DebugActivity">
        </activity>
    </application>

</manifest>